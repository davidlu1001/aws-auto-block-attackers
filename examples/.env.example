# Environment Variables for AWS Auto Block Attackers
# Copy this file to .env and fill in your actual values
# IMPORTANT: DO NOT commit .env to version control!

# -------------------------------------------------------------------
# AWS Configuration
# -------------------------------------------------------------------
# If running on EC2 with an IAM role, these are not needed
# Otherwise, provide AWS credentials:
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
AWS_DEFAULT_REGION=ap-southeast-2

# -------------------------------------------------------------------
# Slack Configuration (Optional but Recommended)
# -------------------------------------------------------------------
# Get your bot token from: https://api.slack.com/apps
# Format: xoxb-...
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token-here

# Get your channel ID from Slack (right-click channel > View channel details)
# Format: C04... or channel name
SLACK_CHANNEL=C04ABCDEFG

# -------------------------------------------------------------------
# IPInfo Configuration (Optional)
# -------------------------------------------------------------------
# Sign up for free at: https://ipinfo.io/signup
# Free tier: 50,000 requests/month
IPINFO_TOKEN=your-ipinfo-token-here

# -------------------------------------------------------------------
# Python Configuration
# -------------------------------------------------------------------
# Add script directory to Python path if needed
PYTHONPATH=/opt/aws-auto-block-attackers

# Python unbuffered output (useful for logging)
PYTHONUNBUFFERED=1

# -------------------------------------------------------------------
# Application Configuration (Optional)
# -------------------------------------------------------------------
# These can also be passed as command-line arguments

# Default AWS region
# AWS_REGION=ap-southeast-2

# Load balancer pattern
# LB_NAME_PATTERN=alb-*

# Scanning threshold
# THRESHOLD=50

# Lookback period
# LOOKBACK=60m

# -------------------------------------------------------------------
# Logging Configuration (Optional)
# -------------------------------------------------------------------
# LOG_LEVEL=INFO
# LOG_FILE=/var/log/auto-block-attackers.log

# -------------------------------------------------------------------
# Security Best Practices
# -------------------------------------------------------------------
# 1. Set restrictive file permissions:
#    chmod 600 .env
#    chown root:root .env
#
# 2. Never commit .env to version control:
#    Add .env to .gitignore
#
# 3. Use AWS IAM roles when possible:
#    Attach role to EC2 instance instead of using access keys
#
# 4. Rotate credentials regularly:
#    Update Slack tokens, AWS keys, and API tokens periodically
#
# 5. Use AWS Secrets Manager or Parameter Store for production:
#    Store sensitive values in AWS Secrets Manager instead of .env
#
# -------------------------------------------------------------------
# Loading this file
# -------------------------------------------------------------------
#
# In systemd service:
#   EnvironmentFile=/opt/aws-auto-block-attackers/.env
#
# In cron:
#   */15 * * * * . /opt/aws-auto-block-attackers/.env && python3 script.py
#
# In bash:
#   source .env
#   export $(cat .env | xargs)
#
# In Python (using python-dotenv):
#   from dotenv import load_dotenv
#   load_dotenv()
